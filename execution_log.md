# v0.3 execution log

Updated: 2026-09-08. This log records executed work and observations; [delivery plan](docs/V0.3.md) records intended gates. Model cards are routing priors, not measured Matterweave competence. Raw worker logs and comparative evidence remain under `/mnt/bench/matterweave-dev/v0.3/`; durable summaries belong here and in release artifacts. Lead owns this log; workers submit short evidence handoffs.

## 01 — Preserve the plan and start coordination bootstrap

- **Actions Taken:** Added v0.3 delivery/board protocol and linked HANDOFF, STATUS and ROADMAP; opened PR #3. Ran documentation checks and the installed coordination helper suite (11 passed). Created isolated board/bootstrap and fixed read-only investigation worktrees at `fc7a939`. Verified ADB is authorized and the installed app is v0.2.0/code 2.
- **Issues & Friction:** Existing helper does not enforce attempt ownership, small aggregate inbox output, applied decisions or process fencing. Its actor identity is self-declared. Existing CI runs host/Android builds even for documentation changes; those checks are pending.
- **Decisions & Rationale:** Dispatch one Astra/high bootstrap worker, plus read-only Muse/high physics and Opus/medium streaming investigations; three workers plus lead is the full current capacity. Feature writers wait for the board gate. Restrict external tools and descendants per invocation.
- **Solutions Applied:** Lead requested direct SQLite CLI plus a small lock holder after the bootstrap worker proposed a Unix-socket supervisor; this reduces coordination infrastructure. Shared app/build/phone resources remain lead-owned.
- **Insights:** The skill provides sound storage primitives but the operational guarantees require explicit implementation and rehearsal. Read-only investigations can proceed while those safeguards are built. No claim of validated board recovery yet.

## 02 — First Muse source investigation

- **Actions Taken:** OpenCode ran exact `opencode/muse-spark-1.3-contributor-free`, requested high, explicit fixed workspace, plan agent with read/search only. Completed in 25.43 seconds, exit 0, two reported step finishes with zero model cost. Captured session `ses_f8248309bffeSgYyFbGjoSkHl0` and source-linked proposal for a breakable wall.
- **Issues & Friction:** Raw events were about 238 KiB despite a concise assignment. The handoff contains a budget-analysis error: seven full cubes fracture to 56 single-voxel bodies, which cannot be fractured again, so its suggested late `56+7` scenario does not follow. Some proposed tests assert incidental mesh counts or duplicate existing coverage.
- **Decisions & Rationale:** Retain useful source mapping and the fully-fracturable bounded-structure idea. Independently verify geometry/physics assumptions and tighten acceptance tests before assigning implementation.
- **Solutions Applied:** Extracted only final prose and per-step usage into lead context; raw tool transcripts stay in artifacts. Marked the handoff partially useful rather than accepting every statement.
- **Insights:** This first real code-reading task supports Muse as a fast source investigator, but not unreviewed correctness authority. It agrees with the card's bounded-task recommendation and its warning that production repair burden is unmeasured. One task is not a comparative benchmark.

## 03 — Opus streaming investigation and integration branch

- **Actions Taken:** Claude Code completed the fixed-source streaming investigation in 96.91 seconds on `claude-opus-5`, medium requested, with no permission denials. PR #3 passed docs/host/Android CI and merged as `b6cb9c4`; created `codex/v0.3-integration`. Backed up the phone's current private app files before test manipulation.
- **Issues & Friction:** Opus reported 96,742 cache-creation and 409,497 cache-read input tokens across calls, despite a short brief; raw events were about 293 KiB. Reported $1.3474965 is list-price equivalent including a Haiku helper, not verified cash spend. Its design's global residency epoch could discard useful retained-chunk work during travel; the near-radius collision argument requires explicit movement/availability guarantees, not a speed assumption.
- **Decisions & Rationale:** Keep single-thread authoritative world ownership, immutable inputs, bounded preparation and synchronous collision publication as useful design constraints. Avoid accepting its Arc conversion and budget guesses without workload evidence. Tighten next-worker startup context and require explicit race/movement rules.
- **Solutions Applied:** Captured final artifact and usage separately from full events. Plan to use Claude safe mode with an explicit project contract/manual instruction reads on the implementation task, preserving subscription authentication, to reduce automatic customization context.
- **Insights:** Complete briefs did produce useful source-specific analysis, as the Opus card suggests, but medium effort did not prevent broad output/context overhead. Model-returned success is not an architectural acceptance check. Native shadow worker proposed a compatible lighting API and optional validated GPU timestamps; no shadow code written yet.

## 04 — Board integration and repeatable device capture

- **Actions Taken:** Started a 120-second warm-up plus 20-minute stationary v0.2 presentation-timestamp collection on the connected OnePlus 13; collector also records battery, thermal-service and process-memory samples. Confirmed settled app focus and frame timestamps advance. Exported Muse session metadata confirming the exact free model and high variant. Prepared bounded core/physics implementation briefs and isolated worktrees.
- **Issues & Friction:** Initial screenshot captured the launcher-to-app animation; a later settled screenshot confirmed the actual scene. Native agent handles are canonical names containing slashes, which the draft board's identifier validator rejected. Root model has no dedicated exposed OS PID, so a shared app daemon cannot be treated as this run's lead process.
- **Decisions & Rationale:** Use a dedicated lock-holder supervisor with root task identity recorded separately. Ask the board worker to accept real opaque native identifiers and test them. Request immutable background world preparation before broad storage refactoring; preserve collision publication and explicit movement gating.
- **Solutions Applied:** Captured the settled screenshot and retained initial capture as a known transition artifact. Sent concrete board compatibility fixes through native bootstrap messages. The arch brief explicitly budgets all constituent voxels to 64 pieces and preserves old save compatibility.
- **Insights:** Even a small cooperative board needs runtime-specific qualification. The extra bootstrap work is real orchestration overhead, not an engine improvement. Presentation timestamps are not GPU execution times; optional renderer GPU queries are a separate assignment. Baseline capture is in progress, not yet a sustained result.

## 05 — Board gate passed and feature writers dispatched

- **Actions Taken:** Integrated board commit `8f53e40` as `0486d53`. Bootstrap worker reported 16 tests passed, with affected cases rerun after final fixes. Lead independently initialized/reconciled the real run, confirmed worker views of 983/1,077/1,009 bytes, and verified rejection of unregistered work and undelivered acknowledgments. Started native shadows, Muse playground and Opus streaming implementation in disjoint worktrees.
- **Issues & Friction:** First Opus launch exited before inference: a variadic CLI option consumed the final prompt. The run needed real process/attempt fencing even for this mundane launcher error. Muse's site proposal assumed home camera (12,18,28) and placed the arch behind the actual (12,10,23) camera; its sampled seeds did not include the app's 20260907 fixture.
- **Decisions & Rationale:** Board current became the live allocation authority. Use a per-worker shorthand client with fixed identity to reduce repetitive JSON, preserving the underlying bounds and checks. Send Claude prompts via stdin. Require actual scene/camera evidence before accepting layout.
- **Solutions Applied:** Verified the failed worker session/children were inactive and its tree unchanged; replaced streaming attempt1 with attempt2 before relaunch. Sent versioned lighting v2 and structure v2 decisions with explicit application gates. Corrected the site to be ahead of the real home camera and requested actual-seed support verification.
- **Insights:** The board has already caught a real lifecycle boundary and carries concise actionable decisions without replaying transcripts. Its 1,272-line implementation/docs/tests footprint is a real setup/maintenance cost. Native roster listing included old completed-task summaries, so routine supervision should use known IDs and targeted board reads. One lead effect pointer predicted the next message ID incorrectly under concurrent sends; subsequent sends use returned IDs, never guessed sequence numbers.

## 06 — Stable submissions, independent review and app integration

- **Actions Taken:** Opened draft PR #4. Verified all three workers inactive with clean submitted revisions before integrating their commits: Muse `38698e0`, Opus `9349d69`, native shadows `21c6e1a`. Recorded board lifecycle as submitted, not accepted. Muse implementation elapsed 576.05 seconds; Opus attempt2 589.14 seconds. Worker package checks reported 16 physics, 28 core and 12 renderer tests passed. Root added shadow/sun/detail controls, async app wiring, bounded upload/request scheduling and prior-completed GPU capture.
- **Issues & Friction:** Muse interpreted the board's successful `submitted` response with `accepted:false` as unexplained failure; it correctly stopped for lead adjudication. Opus source review found late abandoned-destination publication, stream-priority mesh starvation, allocation accounting by length instead of capacity, and old-generation completion deleting a new active marker. Native renderer review found PCF receiver-depth bias insufficient for an unobstructed floor. A third native review dispatch was refused by the actual runtime despite the planned capacity; no extra process was launched to evade it.
- **Decisions & Rationale:** Keep worker source revisions frozen during independent Astra reviews. Lead owns integration repairs; shadow reviewer produces a patch artifact without changing the reviewed checkout. Reuse the finished streaming reviewer for baseline analysis. Submission status is evidence awaiting acceptance, not a rejected submission; board documentation/response should say this explicitly.
- **Solutions Applied:** Root coalesced stream identities, invalidated abandoned destinations, alternated stream/mesh service, charged allocated vector capacity, retained new-generation markers and moved rejection checks before halo copies. App falls back to synchronous preparation if the worker is unavailable; collision publication completes before residency-gated motion. Shadow correction is being prepared for integration.
- **Insights:** Passing package tests did not cover all concurrency or visual seams. The bounded board carried eight small targeted messages instead of raw transcripts, but response wording caused avoidable confusion. Correction to the context-overhead hypothesis: first normal Opus call consumed 7,793 input/cache tokens versus 9,586 for the safe-mode implementation call. Aggregate multi-call cache totals are not peak context size, and these different tasks do not establish safe-mode savings. Broad source reads and repeated cached context contributed to the original aggregate; no model-cost advantage is claimed from this experiment. GPU timestamps can include swapchain-acquire semaphore waiting, so their label must not imply pure active GPU work.

## 07 — Integrated regressions and build gate

- **Actions Taken:** Applied the independently reviewed receiver-plane PCF patch and two geometric-oracle regression tests. Workspace tests passed (29 core, 16 explorer before the added lighting-persistence test, 16 physics, 14 renderer). Clippy passed. Started ARM64 v0.3.0/code3 build. Added saved lighting preferences with old-session defaults and explicit completed GPU frame/settings columns.
- **Issues & Friction:** GPU validation success alone did not detect the flat-floor shadow artifact; a numerical geometric review did. The new persistence test and final device build remain pending at this entry. Baseline analysis found incomplete compositor-history overlaps: large timestamp gaps across missing histories are not confirmed frame stalls.
- **Decisions & Rationale:** Preserve the small shadow contact bias and compensate each PCF tap at the receiver plane/nearest texel center. Keep source-derived bounds separate from measured memory. Clarified successful board submission as `acceptance_state:awaiting_lead_review` to avoid repeating Muse's handoff confusion.
- **Solutions Applied:** Explicitly label the GPU interval as including possible acquire-semaphore wait; retain optional/missing values and associated settings. Baseline analyst separates co-observed presentation intervals from sampling gaps and records thermal status instead of inferring device speed from a single HUD counter.
- **Insights:** Muse's implementation reported 56 steps, zero model cost and 2,955,817 aggregate total tokens (including repeated context); Opus reported 32 turns, $2.5408355 list-equivalent cost, 81,709 cache-created and 1,511,573 cache-read input tokens, 38,619 output tokens, no permission denials. These are harness reports, not verified cash charges or a controlled model comparison. Both delivered useful code; Opus required four concurrency/budget repairs, Muse needed scene correction, and native GPU work needed visual-math repair. Review and integration effort remain part of the real cost.

## 08 — Physical-device findings and collision integration repair

- **Actions Taken:** Installed the first v0.3 APK over v0.2, preserving the saved terrain/camera/11 bodies. Verified actual app focus and new controls; reset creates six arch objects, shadow toggle, sun cycle and 2048 detail respond. Host 90-frame app smoke passed edits/objects/save/reload/resize/recreation with synchronization validation. Final board suite passed16 tests in96.57 seconds.
- **Issues & Friction:** Phone screenshots show diagonal shadow banding on lit vertical terrain faces despite passing shader compilation, geometric tests and host validation. Separate integration review found the old 40m physics activation rule could re-enable an object beyond the still-published window while background generation lagged; character-only residency gating was insufficient.
- **Decisions & Rationale:** Withhold release acceptance until real-device shadow artifacts are corrected. Make body activation depend on the actual published collision window, preserving the reusable physics contract instead of assuming terrain always recenters immediately.
- **Solutions Applied:** Added bounded published-residency keys, cached resident columns in physics, and conservative body-radius/fixed-step activation margins. Added a regression where a nearby preserved body remains frozen beyond the old window and resumes after collision publication. Shadow reviewer is investigating the on-device raster discrepancy against exact on/off captures; no claim of resolved banding yet.
- **Insights:** Platform validation and numerical tests cover different failure classes; neither substitutes for phone visuals. The async core passed its review repairs, but a cross-module assumption in existing physics still needed attention. Hardware testing is directly changing the implementation, not merely confirming worker reports.

## 09 — Correct the shadow diagnosis with an isolated device fixture

- **Actions Taken:** Owner reported interacting with v0.3 (grab/sun/shadow controls); labeled its CSV interactive and excluded it from controlled performance comparisons. Backed up the current user scene before switching to a temporary isolated floating cube. Compared actual phone captures with shadows on/off and low sun/2048.
- **Issues & Friction:** The earlier visual diagnosis was too confident: bands in a complex stepped scene do not by themselves prove self-shadow acne. Replacing hardware comparison sampling with integer depth loads did not remove them.
- **Decisions & Rationale:** Use an exposed +Z cube face with no eligible foreground caster as the discriminating fixture. Preserve current user data and restore it after controlled tests. Retain the receiver-plane correction, but revert the manual-fetch diagnostic because it showed no advantage.
- **Solutions Applied:** The default-sun face interior rectangle (1000,400)-(1400,650) was pixel-identical on/off in RGB (all differences zero). Both default and low-sun isolated faces appeared clean. Reviewer withdrew the ordinary-acne diagnosis; remaining complex-scene bands are recorded as coarse terrace/contact-shadow quality limits of a single128m map (12.5cm texels at1024,6.25cm at2048) and nine-tap filtering.
- **Insights:** Explicitly testing a competing explanation prevented unnecessary bias changes and preserved contact detail. Failed diagnostic hypotheses belong in the execution log with their correction. The first GPU review found a real mathematical PCF defect; later phone-band interpretation was not supported by the isolated evidence. Native reviewers require the same evidence discipline as cheaper workers.
