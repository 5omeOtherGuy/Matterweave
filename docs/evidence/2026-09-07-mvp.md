# M0/M1 local integration evidence — 2026-09-07

## Scope and environment

Implementation source revision: `2cd6435a900fea7868c5e5225a5ff5ac94d99c0d`.
Subsequent commits update evidence/documentation only. Linux x86_64,
Rust/Cargo 1.96.0, local OpenJDK 21.0.12, Gradle 8.11.1, AGP 8.9.2,
NDK 28.2.13676358, platform API 35, build-tools 35.0.0. Debug Cargo profile is
unoptimized with debug information disabled; native APK libraries are stripped
by AGP. No release/device performance result is inferred.

No device was reported by `adb devices -l`. No emulator was configured or run.
The host native smoke used Xvfb and Mesa llvmpipe (LLVM 20.1.2, 256 bits),
Vulkan 1.4.318, raw driver 104865800, vendor 10005/device 0000.
VK_LAYER_KHRONOS_validation was enabled. Seed 20260907. Initial window 1280x768;
exercise requested and observed 1100x700, then recreated the window/renderer.

## Executed verification

Commands are reproducible from [DEVELOPMENT](../DEVELOPMENT.md).

- Workspace formatting and strict Clippy passed.
- `cargo test --workspace --locked`: 14 core and 10 explorer tests passed.
- Core LLVM instrumentation: 94.40% production line coverage; see core README
  for exact commands and exclusions. No GPU/branch-coverage inference.
- Host exercise passed aimed place/remove, save/load, resize event and renderer
  recreation, then completed 30 actual presented frames. World revision after
  exercise 42904. [Raw log](2026-09-07-host-smoke.log) has no Vulkan validation
  warnings or errors. DRI3 notices came from enumerating an unavailable host
  driver; the selected llvmpipe surface subsequently rendered successfully.
- A separate [Xvfb capture](2026-09-07-host.png) was visually inspected: terrain, grove/arch/mineral
  landmarks, depth, shading, fog, crosshair and touch/diagnostic HUD are visible
  and oriented correctly. This screenshot is host evidence only.
- ARM64 APK assembled successfully. A second normal build passed with Gradle
  dependency verification enabled and no metadata-writing option.
- `tools/verify_apk.py` and build-tools `zipalign -c -P 16 -v 4` passed. Native
  library is stored at ZIP offset 16384; every ELF LOAD alignment is 16384.
- `apksigner verify --verbose`: valid debug v2 signature, one signer.
- `aapt dump xmltree` confirms label Matterweave, API 28/35, native library name,
  NativeActivity launcher, ARM64 and Vulkan 1.1 requirement; no app permissions.
- GNU `readelf --dyn-syms -W` confirms exported `android_main` and
  `ANativeActivity_onCreate`. DT_NEEDED contains only liblog, libdl, libandroid,
  libm and libc. Vulkan is loaded dynamically by ash.

## Useful failures and corrections

- Independent review found that requesting current surface pre-transform without
  transforming the scene/HUD would break Android landscape orientation. The
  baseline now requires supported IDENTITY and delegates rotation to compositor.
- An initial Xvfb smoke timed out because an unfocused window did not redraw.
  Explicit smoke mode now continues drawing while ordinary app focus pause remains;
  input cancellation is preserved and the behavior has a regression test.
- Corrupt-save recovery originally risked replacing the unreadable original on
  first edit. Numbered recovery sidecars now preserve original bytes and existing
  invalid sidecars, and restore the latest valid recovery on restart.
- First Gradle task discovery did not exercise aapt2 downloads, leaving two
  missing checksums for actual assembly. Their trusted Google artifacts were
  added to verification metadata, followed by a normal verification-enforced build.
- The first copied Gradle distribution checksum accidentally included a character
  from adjacent command output; wrapper verification rejected it. Corrected it
  against the official distribution checksum and rebuilt successfully.
- NDK llvm-readelf crashed while printing the full unstripped symbol table.
  GNU readelf successfully inspected the immutable library extracted from the
  APK; this tool failure is not recorded as a passed NDK inspection.

## Outstanding evidence

Physical Android launch, real multitouch, background/resume, lock/unlock,
orientation, surface recovery, low-memory behavior and driver validation are
not run. No GPU timestamps, sustained frame-time/thermal/RSS/power benchmarks,
LOD/lighting quality comparison or renderer-performance selection is claimed.
Remote GitHub Actions has not run. Current status and next actions are in
[STATUS](../STATUS.md).

## Fresh-checkout verification

A detached checkout of `2cd6435a900fea7868c5e5225a5ff5ac94d99c0d` built from an
empty Cargo target directory. The pinned SDK and downloaded Gradle/Cargo caches
were reused; no object or app build output was reused. Normal Gradle dependency
verification was enabled. All 33 Gradle tasks executed and assembly passed.
[Build log](2026-09-07-clean-build.log) records the actual command output.

The fresh APK passed signing, ELF and ZIP alignment checks again. Its SHA-256
was identical to the working-checkout APK in this environment with the same
local debug signing key: `ca2333d15aa495236ee18d5367f4f2bf9aa46efaf898821e9d524ce702216196`. This is an observed match, not a guarantee
across different signing keys or toolchains. [Artifact manifest](2026-09-07-apk.json)
records the exact 2625049 bytes and build revision.

The APK was copied to `artifacts/matterweave-arm64-debug.apk` in the active
checkout. Temporary checkout/build targets were removed after build completion;
shared dependency caches and active-checkout targets were preserved.
