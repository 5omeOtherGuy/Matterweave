# Campaign model outcomes

This is an outcome ledger, not a live assignment board or a model ranking. Current
ownership is only in [board.json](board.json). Pi 0.85.1 exact-selection checks passed
for all six configured families. Catalog selection does not prove authentication,
quota, task qualification or image-input capability.

| Route / effort | Real work and observed outcome | Acceptance / limits |
| --- | --- | --- |
| Muse Contributor Free / high | P00 validator implementation; deliberate cancellation retained partials; exact-session replacement completed. Independent P00/P01 reviews. | P00 accepted after lead correction and delta reviews; 31 tests. Fixture/replay slice now assigned separately. No phone or visual qualification claimed. |
| Opus 5 subscription / medium | P01 typed instrumentation, clocks and counters. First handoff completed; lead host checks passed 87 tests and ARM64 build/alignment. | Candidate-01 not accepted: reviews found wait attribution, retry shadow counts and completion-label issues. One bounded correction assigned. Device overhead still unqualified. |
| GLM 5.3 Flash Go / high | Readiness validator with 60 host tests. Lead reran tests and validated actual timestamped cooling-01/02 records. | Independent reviews pending. Worker used real dumps with synthetic timing in its own demonstration; lead replaced that with actual recorded elapsed times. No app-performance claim from idle readiness. |
| Gemini 3.8 Flash Antigravity / high | Independent P00 and P01 source reviews; identified artifact-type/test gaps, earlier upload-fence attribution and stale retry shadow counts. | Candidate findings checked by lead, not accepted by vote. No image-input or visual qualification claimed. |
| Astra lead | Integration, actual Pi cancellation/replacement, independent finding verification, physical phone setup and baseline collection. | Lead session initially reported medium rather than required high; switch requested, not yet verified. No separate Pi lifetime audit assigned yet. |
| Hy4 OpenRouter / high | Exact route check only; read-only account balance check. | Not task-qualified. Owner authorized $9.99 existing credit; actual balance $9.985349664. No key-level cap; paid execution pending enforceable ceiling/no-top-up verification. |

Raw sessions/results: `/mnt/bench/matterweave-dev/performance/run-01/`. Engineering
logs and accepted findings are preserved in this directory's `logs` subdirectory;
raw provider sessions must not be published. Cancelled attempts remain distinct
from provider failures. Completion means a model turn finished, not code acceptance.

Muse uses the exact Contributor Free SKU. Claude/Gemini zero cost fields are
provider placeholders, not free usage. Go catalog prices differ from published
rates; actual subscription depletion is unknown. No paid inference, purchases,
automatic top-ups or paid fallback has been performed in this campaign so far.
All-model task qualification remains incomplete; no equal-spend or cross-task speed
ranking is inferred from these unequal assignments.
