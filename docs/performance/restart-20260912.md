# Restart checkpoint — 2026-09-12

Owner requested a computer restart after HDD clicking and I/O failure. All three
latest Pi workers completed naturally; none was stopped. No cargo/Gradle build
was running at checkpoint. Do not launch workers until Git and Pi are reconciled.

## Safe working locations and preserved source

Use `/home/someotherguy/Documents/ChatGPT/Matterweave-recovery-20260912/`.
The original checkout and Git object database are on the healthy SSD. Preserve its
seven pre-existing untracked contributor logs; do not reset or clean that checkout.

| Checkout / branch | Frozen source | State and next gate |
| --- | --- | --- |
| integration / engine/recovered-mesh-lighting | b38dcea plus this checkpoint | Mesh-aware upload API; draft delivery requires independent integration review and positive GPU/Android fixture |
| detail / engine/recovered-detail | 05549eb3afc2e33bc2ecad6a55a72a10e3d8b4cc | DeepSeek repair: 20 focused, 158 app tests; 14 host Vulkan phases reported passing. Lead diff acceptance, independent corrective review and Android NOT RUN |
| streaming / engine/recovered-streaming | f63055b23b02c9e8c7f63447dac8b8ac9f38d16f | DeepSeek byte-admission discriminator and bounds corrections; core suite/Clippy/fmt pass reported. Lead acceptance and integration/device gates pending; sustained-edit app liveness OPEN |
| lighting-response / engine/recovered-dynamic-lighting | c452f8129d1ddd26641e68642e0c25bf2a3af3d3 | Muse reconstructed response/completion gate; 107 renderer tests reported passing. Gemini w_1e2f7a50 returned zero findings; lead acceptance and Android integration still pending |

Detailed worker logs are committed in each source branch under
`docs/performance/logs/`. Raw results and briefs remain on the healthy SSD under
`orchestration/{detail-repair-openrouter,streaming-repair-openrouter,lighting-response-muse,lighting-response-review}`.
Worker completion and a zero-finding review do not themselves mean lead acceptance.
No new milestone is closed by this checkpoint.

## Storage and build precautions

One owner-authorized software rescan failed at 21:26:32: READ CAPACITY returned
DID_BAD_TARGET; the kernel changed sdb capacity to zero. This is failed device
communication, not evidence of erased data. Owner reports clicking. Do not resume
writes, scans, filesystem repairs or builds on /mnt/bench merely because it appears
after reboot. No destructive recovery operation was attempted.

Pinned Android SDK/NDK installation completed at `android-sdk` inside the recovery
root. Use a healthy recovery Gradle home and isolated Cargo targets. Set
`RUSTC_WRAPPER=` explicitly: the global sccache setup otherwise references the failed
/mnt/bench cache, as the detail worker discovered. Do not delete shared caches.
Benchmark campaigns remain deferred; do not relocate benchmark data onto the SSD.

## Resume sequence

1. Read AGENTS, HANDOFF, STATUS, completion plans and this checkpoint. Reconcile
   actual branches, open PRs and Pi registry before assigning anything.
2. Keep DeepSeek V4.1 Flash on OpenRouter and exact Muse Spark 1.3 Contributor Free;
   Gemini independent reviews, global three-worker cap, NEVER Astra workers.
3. Inspect and accept/repair the three frozen deliveries, then complete review,
   integration, relevant Android gates and passing PR merges. Do not overwrite old
   failed-drive worktrees or duplicate existing detail/runtime wiring.
4. The clean CI APK from PR35 is downloaded in `orchestration/main-apk`, NOT installed.
   Verify checksum/signing, use the existing local debug key if necessary, install
   with replacement only and preserve current saves. Never uninstall/clear data.
   Fresh private save backups are required before new device fixture work.
5. Phone belongs to lead; audio hearing is owner-confirmed. Physical simultaneous
   touch and lock/unlock remain NOT RUN. Phase B performance/thermal work is deferred.

PR35 remains merged at ac1ce444, with six green checks and committed physical
64-piece/20-cycle evidence. D3.3, D4.2/4.3 and E8-T remain queued. The mesh proxy's
subcell digest and fractional self-hit limits remain disclosed and unresolved.
