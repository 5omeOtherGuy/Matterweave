# Engineering log w_b429a845

Worker: `deepseek-flash-go` · outcome: Category: implement · run: `/mnt/bench/matterweave-dev/microvoxel-roadmap/fixtures`

- **Actions Taken:** Extended `crates/matterweave-render/examples/renderer_comparison.rs` with the opt-in `--landscape` flag and two functional fixtures, `vegetation-vs-distant-ridge` and `negative-ortho-opening-edit`, reusing the existing raster/ray/hybrid subpasses, CPU oracle, edge/face-edge gates, marker, edit and pack-invalidation checks. Default invocation still runs exactly the original 16-run gate; `--landscape` appends 8 runs (24 total). Added GPU-free unit tests for RayVolume bounds, no silent crop, split partitioning of near detail, edit placement and fixture-set stability. Ran under lavapipe with validation: default `16 runs, 0 failures`; `--landscape`: `24 runs, 0 failures`; tests 4/4 pass; scoped fmt and Clippy clean. Wrote `docs/performance/landscape-functional-fixtures.md`. Raw logs/artifacts under `/mnt/bench/matterweave-dev/microvoxel-roadmap/fixtures/`.
- **Issues & Friction:** The first `--landscape` run failed 2 matched landscape runs (fixture 2) on exactly 116 pixels in image row 0: raster covered a top-row band at depth 0.55442 (50.0 world units) while the pixel-centre ray saw background. Also hit a compile error (`rem_euclid` on an ambiguous integer range, fixed with an explicit `i32` bound) and a self-inflicted test bug (asserting an edit cell must pre-exist, which an addition cannot satisfy).
- **Decisions & Rationale:** Kept the default suite and output behavior byte-for-byte unchanged by making landscape fixtures additive and opt-in. Marked fixture 1's near-detail AABB so the sparse oracle must report at least one hit inside it (observed 35-84), preventing the ridge/terrain from satisfying the checks alone. Chose split columns that leave both paths with visible geometry and keep the edit off the dropped column. Fixed the row-0 failure by changing framing (extending the back wall to `y = -2..=8` so row 0 hits the wall's front face with >1 world unit of margin and the wall's top edge is outside the frame), not by loosening the gate: the mismatch was a genuine projected-edge degeneracy where the wall's top surface lay 0.0016 px from the row-0 pixel centre, so raster float fill covered the pixel while the pixel-centre ray passed 0.0009 units behind the wall. The gate correctly refused to excuse background as a two-face edge.
- **Solutions Applied:** Preserved the failed log, summary and images unchanged in `fixtures/landscape-failed-toprow116/` with a README recording the measured root cause and the fix; re-ran to 24/24 pass. Added `detail_hits` to the oracle report so thin near geometry is explicitly sampled. Added tests proving fixture packs fit `MAX_AXIS = 128` and `MAX_CELLS = 64^3` with no cropped cells. Confirmed the edit patches are in-frame: fixture 1 changes 245 px at `x 90..107, y 90..103`; fixture 2 changes 24 px at `x 64..67, y 68..73`.
- **Insights:** A 128x128 frame at 4-13 px per cell is enough for silhouette-ordering stress, but exact projected-edge/pixel-centre coincidences surface as hard failures rather than tolerated noise: this harness deliberately does not excuse missing coverage. Keep future fixture framing so outer silhouettes are either clearly inside the frame or clearly outside it; avoid placing a horizontal terrain edge within ~0.05 px of a pixel-centre row. The same harness now covers thin near detail and orthographic negative-coordinate edits without touching the default CI gate.

Lead qualification: worker source correction removes an erroneous nonzero-material
assertion; the independent final review caught the frozen earlier version. Unit tests
now pass 4/4. The projected-edge root-cause narrative above is a worker explanation,
not an independently established renderer diagnosis; preserve the original regression.
