# P00 execution and lead engineering log

Device: OnePlus `CPH2653`, Android 16, display build `BP4A.251205.006`. Baseline
release v0.3.0, source `2ce6ce3157e791ab6dd49ed00cd865ee5c1cacc6`, APK SHA-256
`352ce87ed6c667892b876c3026a039959903ff1e529901f92e4879d7058c7ba7`.

Non-claims: the idle-readiness samples are not application performance; route
selection checks are not authentication, remaining quota or task qualification; no
engine optimization, accepted app performance measurement or showcase content is
claimed; P01 instrumentation is frozen for review, not accepted.

## Actions Taken

- Created isolated branch/worktree `codex/performance-p00` at
  `/mnt/bench/matterweave-dev/worktrees/performance-p00`, starting from documentation
  branch revision `755620472e8457d8e55760165e759b6d8013c6db`. Owner's original checkout
  is untouched. No pre-existing changes were present in the new checkout.
- Read the current orchestration skill, profiles, execution helper guidance,
  campaign/tasks/showcase and benchmark requirements. Selected one lead-written
  compact board; left the SQLite committed-submission protocol unchanged.
- Dispatched Muse Contributor Free high through Pi 0.85.1 to implement the bounded
  patch admission validator with RED/GREEN and cancellation/stale-artifact fixtures.
  Exact run/session/ownership is in the sole board. This is implementation, not
  qualification by an identity probe.
- Verified Rust 1.96.0, Cargo 1.96.0, JDK 21.0.12, Pi 0.85.1. `/mnt/bench` reported
  180 GiB available. No existing worker/Cargo/Gradle process was found in the initial
  scoped process inventory. Builds and raw sessions stay under `/mnt/bench`.
- Verified remote `v0.3.0` tag with `git ls-remote --tags origin v0.3.0`. Original
  APK SHA-256 is `352ce87ed6c667892b876c3026a039959903ff1e529901f92e4879d7058c7ba7`;
  `sha256sum` matched the existing release manifest. Source is
  `2ce6ce3157e791ab6dd49ed00cd865ee5c1cacc6`. Local artifact and manifest are under
  `/mnt/bench/matterweave-dev/v0.3/release`; public release is
  <https://github.com/5omeOtherGuy/Matterweave/releases/tag/v0.3.0>.
- Ran non-inference route checks for Muse, Opus, GLM, Gemini, Hy4 and Astra-worker.
  All selected the exact configured provider/model/effort. Raw results reside at
  `/mnt/bench/matterweave-dev/performance/run-01/routes/PROFILE/result.json`.
  These establish selection, **not** working authentication, remaining subscription
  quota or task qualification. Unknown capacity is not unlimited capacity.

## Issues & Friction

- `adb` is not on PATH. The existing SDK's
  `/mnt/bench/matterweave-dev/android-sdk/platform-tools/adb devices -l` detects the
  USB device but reports `no permissions`; USB node is root-owned mode 0664.
  `adb mdns services` found no wireless route. No privilege escalation, permission
  changes, pairing, battery-service simulation or device mutations were performed.
- Device gate subsequently resolved with the owner's permission: temporary user-only
  USB-node ACL, then ADB server restart cleared the cached permission error. A replug
  changed the device node, so the original ACL did not apply to the new node. No
  persistent udev rule or world-writable permission was installed. TLS wireless ADB
  connected after the owner enabled Wireless debugging; the owner physically unplugged
  USB. Battery service then reported AC/USB/wireless/dock power false, current 0 and
  discharging status. Do not use cached thermal entries: current HAL readings differ.
- Backed up current save after Home/suspend: 279365-byte valid JSON, SHA-256
  `ca8a13a88f814374c57943543d6410b034047b97bb466206da2e6e7095f187af`. Verified installed
  APK by pulling and hashing it; it matches the original baseline release. Device
  model CPH2653, Android 16, display build BP4A.251205.006. Baseline save seed 20260907,
  generator 1, revision 42942. Save and raw private device records reside under
  `/mnt/bench/matterweave-dev/performance/run-01/device`, not public Git.
- Stopped six background updater/weather packages and Android-cached apps under
  owner's instruction. Preserved system, security/VPN and communication services.
  Recorded process inventories privately. Original brightness 6/automatic mode 1;
  requested fixed brightness 6 and 60 Hz min/peak for testing, retaining original
  values (refresh keys absent). Pointer/touch overlays were enabled; saved and disabled
  them for subsequent controlled runs. Screen timeout was 1800000 ms; owner requested
  continued access, so set 7200000 ms, verified effective with no device-policy cap.
  Secure lock remains enabled. Restore original display/timeout/overlay settings at
  device-test closeout; stopped updater apps remain stopped until reopened.
- First idle readiness observation: five samples spanning 120 seconds, battery
  29.9–30.0°C, current skin 30.169–30.464°C, thermal status 0 and external power false
  throughout. This is **idle readiness, not application performance**. A subsequent
  baseline-01 attempt was rejected: foreground check found launcher, then a replaced
  surface layer produced no valid timestamps. The user correctly queried whether the
  app was running. No causal explanation for the foreground transition is established.
  Relaunch screenshot confirmed the app; querying its new layer showed two advancing
  timestamp histories (63 entries each). Collector now rejects empty frozen-layer
  histories and checks foreground/power; re-cooling and recollection follow.
- Lead session environment reported Astra medium, not the requested high. Asked
  owner to switch thinking level; do not record this as high execution until verified.
- No local `v0.3.0` tag existed; verified the remote tag instead. An earlier chained
  toolchain command stopped at that missing tag; reran toolchain checks explicitly.

## Decisions & Rationale

- Use the explicitly permitted lead-written board option instead of weakening the
  old clean-commit submission check. A tiny stateless validator adds testable stale
  attempt/hash rejection without creating another orchestration service.
- Reuse Python stdlib argparse/json/hashlib/pathlib for CLI, serialization and hashes.
  The existing SQLite board serves a different committed-worktree contract and is
  deliberately not rewritten. No new dependency or copied private skill code.
- Reuse the installed Android SDK/Gradle cache read-only/as normal build caches.
  Use a campaign target directory, serialize heavy builds, and remove temporary
  targets only after their processes stop. Preserve existing worktrees and caches.
- Owner explicitly authorized the entire existing OpenRouter balance, stated $9.99,
  for Hy4. Campaign and maximum single-attempt ceiling: $9.99, further limited by
  actual remaining prepaid credit. No purchases, top-ups or paid fallback authorized.
  Read-only OpenRouter key/credit queries returned total credit $10, usage
  $0.014650336, remaining $9.985349664; key limit and limit_remaining are null.
  Sanitized records are under the run directory; no credentials are recorded.
  Hy4 inference remains pending until an enforceable ceiling/no-top-up route is
  verified. Pi timeouts are not a cash cap. Subscription remaining quotas unknown.

## Solutions Applied

Separate checkout, disjoint file ownership, exact Pi profiles and opaque hashed patch
handoffs. Keep unavailable device and paid execution gates explicit while independent
host implementation proceeds. Never claim the old powered/hot phone run as a causal
baseline for this campaign.

## Insights / current gates

P00's host handoff implementation is accepted after independent Muse/Gemini reviews,
lead inspection and 31 passing tests. Real Pi cancellation was
rehearsed on Muse attempt 1 after both test and implementation files existed.
SIGINT did not stop the background runner: the shell launch inherited ignored
SIGINT. Inspected the helper's signal handler, then used its handled SIGTERM.
Runner 359107 stopped and no live member remained in its Pi child group/session
359108. Both partial files were copied before replacement, with SHA-256 records at
`/mnt/bench/matterweave-dev/performance/run-01/cancellation-partial-sha256.json`.
Attempt 2 uses owner `muse-p00-a2`, a fresh run directory and the exact idle attempt-1
session; no overlapping writer was started. The failed SIGINT attempt released no
ownership. Automated stale-output rejection passed against the actual replacement
board as well as fixtures. Muse found no primary defect; Gemini identified masked
board-side tests and an API artifact-type escape. Lead reproduced three invalid-type
failures, added a Path guard (prevents accidental descriptor 0 access), and expanded
tests to 31 passing. Both independent delta reviews found no further defect. Deep
JSON recursion was considered but not reproduced at 1100/3900 nesting on this Python;
no recursion fix is claimed. Final code/test hashes are in candidate-02/sha256.json.

P01 instrumentation is implemented by Opus and frozen for two Muse plus one Gemini
reviews; it is not yet accepted. Baseline workspace tests passed before engine edits.
No engine optimization, accepted app performance measurement, showcase content or
performance improvement is claimed yet. Paid-cap enforcement, fixture/validator work
and instrumentation overhead qualification remain explicit open gates.
