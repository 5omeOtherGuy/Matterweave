# Development recovery — 2026-09-12

PR #35 merged at `ac1ce4442fd7072eca2253916ff625570267b0d7`, after all six
checks passed. Its Android destruction evidence is in Git and the pull request;
the installed corrected-source APK completed 1501 presentations and 20 reset/load
cycles, with four current user-save hashes unchanged. Owner hearing confirmation
is accepted. No later storage failure reverses that observed result.

## Storage failure and preservation

At approximately 20:53 Europe/Berlin, the drive backing `/mnt/bench` began returning
`DID_BAD_TARGET` and I/O errors. `/mnt/bench` is ext4 on `/dev/loop0`, backed by
`/mnt/Data/iris-bench/bench.img` on `sdb2`. Kernel messages include failed extents and
journal superblock writes. Git source reads produced a bus error and individual
file reads returned EIO. No unmount, repair, formatting, reset or cleanup was done.
The owner was asked to check the connection. Do not treat the old filesystem as
healthy simply because a cached file remains readable.

The main Git object database lives on the healthy system disk. Committed D1.2
`d8a5cde` and D2.3 `5e5c290` were pushed to their existing remote branches for
preservation. The original failed-drive worktrees remain intact for recovery.
A best-effort source-only salvage is at
`/home/someotherguy/Documents/ChatGPT/Matterweave-source-recovery-20260912`, with a
SHA-256 manifest and explicit read errors. Recovered: lead renderer `lib.rs` and
reflection example; D3.2 modified `indirect_tests.rs`. Three mechanical lead caller
adaptations were recreated from committed source. D3.2 modified `indirect.rs` and
`reflection.rs` were unreadable; their changes are not claimed recovered. Muse
works from the committed D3.1 files plus the saved test source to reconstruct the
bounded response slice. The original versions must not be overwritten.

No active Pi workers or cargo/Gradle builds remain on the failed drive. New source
checkouts and ordinary development tests use
`/home/someotherguy/Documents/ChatGPT/Matterweave-recovery-20260912/`:

| Checkout | Branch / responsibility |
| --- | --- |
| `integration` | `engine/recovered-mesh-lighting`; lead API integration and acceptance |
| `detail` | `engine/recovered-detail`; bounded detail diagnostic repair |
| `streaming` | `engine/recovered-streaming`; bounds/test/log corrections after review |
| `lighting-response` | `engine/recovered-dynamic-lighting`; preserved D3.2 reconstruction |

Only ordinary development source/checks moved. No benchmark campaign or benchmark
data is written outside `/mnt/bench`. Keep per-worker Cargo targets separate and
one build job per worker; clean only owned inactive recovery targets afterward.
Android SDK/NDK and old raw campaign logs on the failed drive are currently
unavailable. Reestablish the pinned toolchain before claiming any new local APK
build. The existing installed APK and GitHub CI delivery remain available.

## Provider recovery

Both Go workers received `429 GoUsageLimitError`, explicitly saying weekly usage
was exhausted and resets in one day. No paid overage or top-up was enabled. The
owner directed use of existing OpenRouter credits and Muse Spark 1.3 Free before
Opus. Canonical orchestration profiles now include `deepseek-flash-openrouter`,
exact model `deepseek/deepseek-v4.1-flash`. Pi selection checks and real source-tool
calls succeeded. Effort is `high`: the installed Pi mapping cannot select `max`,
so no Go/max equivalence is asserted. Exact Muse free model is
`muse-spark-1.3-contributor-free`, high. No Opus worker was dispatched; Astra
workers remain prohibited. Remaining credit balance is not inferred.

Live assignments are in the board. Keep the global three-worker cap, including
Gemini reviews. D3.3, D4.2/4.3 and E8-T remain queued rather than implemented.
M5/Phase B remains deferred. Physical simultaneous touch and lock/unlock remain
NOT RUN; ADB sequential gestures do not close those human/device rows.

## Restart checkpoint

The pinned Android SDK/NDK has now been restored on the healthy SSD. Workers have
finished; see [restart handoff](restart-20260912.md) for commits, checks and pending
acceptance. The subsequent software rescan failed; no filesystem repair was run.
