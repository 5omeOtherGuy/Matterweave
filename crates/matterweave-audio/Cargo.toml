[package]
name = "matterweave-audio"
version.workspace = true
edition = "2021"
publish = false

[dependencies]

[target.'cfg(target_os = "android")'.dependencies]
ndk = { version = "=0.9.0", features = ["audio", "api-level-28"] }
