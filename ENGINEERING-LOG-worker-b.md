# Engineering log w_18695d98

Worker: `muse-free` · outcome: Worker B brief — landscape flora catalogue (S3a) · run: `/home/someotherguy/.pi/agent/subagent-dashboard/runs/w_18695d98`

- **Actions Taken:** Added detail materials 60..=72 (all Decorative) with names/colours/policies; new `landscape_flora` module with 21 voxel prototypes (grass x3, flowers x9, fern, shrub, cactus, broadleaf x3, conifer x3) plus `prototype_for`/`landscape_prototype`/`landscape_flora_class` bridge over `FloraKind`; `landscape_flora_gallery` example printing real meshed cells/verts/tris and failing over cap; `tests/landscape_flora.rs` (10 tests: dispatch, caps, determinism, policy, connectivity, LOD, placement, budget); extended explorer `CATALOGUE_MATERIALS` to 40 entries. Merged parent `origin/engine/landscape-generator` (d90db40) mid-work, dropping my redundant clippy stash.
- **Issues & Friction:** Thin 1-cell geometry meshes ~4-7 tris/cell, so first-cut flowers (122 tris) and fern (414 tris) blew the caps; hollow leaf shells isolate pole cells (shrub split into pieces); seed 20260913 has no desert within ±960 m (switched placement seed to 7); explorer palette test enumerates material IDs and failed until extended; workspace clippy still fails on pre-existing vendored winit lint (untouched, out of scope); large tool outputs intermittently swallowed by the harness — verified via log files.
- **Decisions & Rationale:** All prototypes at 12.5 cm including trees (large broadleaf ~3.5 m fits caps, no 25 cm exception needed); solid (not hollow) canopies — pole cells of hollow shells disconnect and solids mesh fewer tris; flower heart as cross centre (shares 4 faces, saves ~8 tris); size variants differentiate by petals/leaves where stems would collide (strict 2..4 stems + distinct colour mediums + 9 distinct shapes cannot all hold otherwise — only 3 integer heights exist); fern arches use one height step so flat runs merge; per-16-m budget 516 is structural (64x8+4), not statistical.
- **Solutions Applied:** Flower redesign (single-cell pad, sepal leaves, heart-centre cross): 128→60-68 tris. Fern redesign (raised crown, single touchdown step, one flank per frond): 414→226 tris, 105→59 cells. Grass large trimmed to 4 blades: 42→34 cells. Explorer catalogue extended 27→40 entries (new colours verified injective by the existing test).
- **Insights:** Greedy-mesh triangle cost is dominated by y-steps and isolated cells, not cell count — design for long flat runs. `landscape_prototype` dispatches on id so determinism tests must call it twice with the same id (unlike direct builders). Desert cactus is ~0.1%/column, so placement tests must scan boundedly (~7 squares at seed 7) rather than assert per-square.
